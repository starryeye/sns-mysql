spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sns-jpa?rewriteBatchedStatements=true&profileSQL=true&logger=Slf4JLogger&maxQuerySizeToLog=999999&serverTimezone=UTC&characterEncoding=UTF-8
#    url: jdbc:mysql://localhost:3306/sns?rewriteBatchedStatements=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: sns-svc
    password: sns-pass

logging:
  level:
    org:
      springframework:
        jdbc:
          core: TRACE

# https://junit.org/junit5/docs/snapshot/user-guide/index.html#writing-tests-parallel-execution
#junit:
#  jupiter:
#    execution:
#      parallel:
#        enabled: true
#        mode:
#          default: concurrent
#        config:
#          strategy: fixed
#          fixed:
#            parallelism: 4